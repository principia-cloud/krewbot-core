// Core stub. Self-hosted deployments have no billing — the Settings
// page omits the billing card entirely. An overlay can swap this
// slot to render its own billing card (e.g. a Stripe-backed view).
export function BillingSection() {
  return null;
}
