// Core default: workspace creation is enabled. Single-tenant overlays
// (e.g. a single-tenant overlay) replace this impl with `false` to hide every
// create-workspace surface (sidebar switcher button, /onboarding form,
// dashboard zero-workspace redirect).
export const WORKSPACE_CREATION_ENABLED = true;
